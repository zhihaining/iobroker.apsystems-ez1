import type { Answers } from "./lib/core/questions";
import type { File } from "./lib/createAdapter";
/**
 *
 * @param answers
 * @param disableValidation
 */
export declare function createAdapter(answers: Answers, disableValidation?: (keyof Answers)[]): Promise<File[]>;
