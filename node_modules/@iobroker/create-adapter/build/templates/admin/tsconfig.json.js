"use strict";
const createAdapter_1 = require("../../src/lib/createAdapter");
module.exports = (answers => {
    const useTypeScript = answers.language === "TypeScript";
    const useTypeChecking = answers.tools && answers.tools.indexOf("type checking") > -1;
    const useJsonConfig = answers.adminUi === "json";
    const noConfig = answers.adminUi === "none";
    if ((!useTypeScript && !useTypeChecking) || useJsonConfig || noConfig) {
        return;
    }
    const useReact = answers.adminUi === "react" || answers.tabReact === "yes";
    return (0, createAdapter_1.readFile)(useReact ? (useTypeScript ? "tsconfig_TS-React.raw.json" : "tsconfig_JS-React.raw.json") : "tsconfig.raw.json", __dirname);
});
//# sourceMappingURL=tsconfig.json.js.map