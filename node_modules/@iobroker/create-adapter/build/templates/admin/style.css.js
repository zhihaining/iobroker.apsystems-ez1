"use strict";
const createAdapter_1 = require("../../src/lib/createAdapter");
module.exports = (answers => {
    const isAdapter = answers.features.indexOf("adapter") > -1;
    const useJsonConfig = answers.adminUi === "json";
    const noConfig = answers.adminUi === "none";
    if (!isAdapter || useJsonConfig || noConfig) {
        return;
    }
    return (0, createAdapter_1.readFile)("style.raw.css", __dirname);
});
//# sourceMappingURL=style.css.js.map